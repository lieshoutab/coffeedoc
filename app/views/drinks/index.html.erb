<h1>Cool Beans</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @drinks.each do |drink| %>
      <tr>
        <td><%= link_to drink.name, drink_photos_path(drink) %></td>
        <td><%= link_to 'Edit', edit_drink_path(drink) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Coffee', new_drink_path%>
